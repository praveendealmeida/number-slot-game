# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# typescript
*.tsbuildinfo

# prisma — schema.prisma is committed; the generated client is committed too
# (it lives in src/generated/prisma). Remove the next line if you prefer to
# regenerate it on install instead of tracking it in git.
# /src/generated/prisma
